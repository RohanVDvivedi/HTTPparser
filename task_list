 * build a function to get its q value

 * check if path matches regex containing only *, after it is split by "/" by using ` for_each_split_by_delim(split, &str, &delim) `
 * implement get content_type for extension (default to text)

 * compare mimetype as given in accept header of the request

 * implement stream to read/write websocket data
 * parser for reading and writing form data 

 * add cookie struct and a function to parse it and serialize it, its values are separated by ";"

 * write a function to initialize zlib_streams from ConnMan based on the http headers